{
	"name": "Alpine",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"forwardPorts": [
		3000,
		6006
	],
	
	"onCreateCommand": "git pull && pnpm install",
	"postCreateCommand": "git pull && pnpm install"
}
